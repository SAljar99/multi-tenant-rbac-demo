rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: Open rules for demo only. Do NOT use in production.
      allow read, write: if true;
    }
  }
}
